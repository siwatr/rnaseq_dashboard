% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_lookup.R
\name{resolve_feature}
\alias{resolve_feature}
\title{Resolve a single query to a feature id, with a match count}
\usage{
resolve_feature(query, values, ids = values, case_insensitive = FALSE)
}
\arguments{
\item{query}{A single search string.}

\item{values}{Character vector of searchable values, one per feature.}

\item{ids}{Feature ids aligned to \code{values} (default \code{values}).}

\item{case_insensitive}{Match case-insensitively (default \code{FALSE}).}
}
\value{
A list: \code{id} (first matching id, or \code{NA_character_}), \code{match} (the
actual matched value as stored -- e.g. \code{"Duxf3"} for a case-insensitive
query \code{"duxf3"} -- or \code{NA_character_}), and \code{n} (number of matches).
}
\description{
Searches \code{query} against \code{values} (a per-feature character vector, e.g. a
\code{rowData} column or the feature ids themselves) and returns the \emph{first}
matching id together with how many features matched -- so callers can warn
"N features matched; showing the first" on a duplicate hit.
}
