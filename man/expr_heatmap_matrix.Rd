% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr_helpers.R
\name{expr_heatmap_matrix}
\alias{expr_heatmap_matrix}
\title{Prepare a gene-set expression matrix for the heatmap}
\usage{
expr_heatmap_matrix(
  mat,
  ids,
  counts = NULL,
  zscore = TRUE,
  only_expressed = TRUE,
  transform = "none",
  pseudocount = 1
)
}
\arguments{
\item{mat}{A numeric genes x samples value matrix (e.g. from
\code{\link[=expr_value_matrix]{expr_value_matrix()}}).}

\item{ids}{Authored gene-set ids (matched against \code{rownames(mat)}).}

\item{counts}{Optional raw counts matrix for the \code{only_expressed} filter.}

\item{zscore}{Per-gene z-score across samples (default TRUE).}

\item{only_expressed}{Drop genes with all-zero counts (default TRUE).}

\item{transform}{Passed to \code{\link[=expr_transform]{expr_transform()}}, applied before z-scoring.}

\item{pseudocount}{Added before a log transform.}
}
\value{
A list: \code{mat} (the prepared genes x samples matrix, or \code{NULL} when no
gene survives), \code{n_total}, \code{n_present}, \code{n_absent}, \code{n_used}, \code{n_nonvar}
(constant kept rows), \code{ids_used}, \code{zscored}.
}
\description{
The heatmap counterpart of \code{\link[=expr_set_aggregate]{expr_set_aggregate()}}: it selects the same gene-set
rows (present in the dataset, optionally dropping all-zero-count genes),
transforms them, and -- by default -- per-gene z-scores across samples so genes
on very different expression scales are comparable. Unlike the aggregate helper
it keeps the full genes x samples matrix (no averaging). Constant (zero-variance)
rows z-score to 0 (via \code{\link[=row_zscore]{row_zscore()}}) rather than \code{NaN}, so they still draw as a
flat mid-colour row instead of breaking the heatmap; their count is returned so
the caller can note it.
}
