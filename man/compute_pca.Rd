% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimreduc_helpers.R
\name{compute_pca}
\alias{compute_pca}
\title{Compute a PCA embedding from a value matrix}
\usage{
compute_pca(mat, n_top = 500)
}
\arguments{
\item{mat}{A genes×samples numeric matrix (endogenous-only).}

\item{n_top}{Number of top-variable features (default 500).}
}
\value{
A list: \code{scores} (data.frame, rows = samples, cols \code{PC1..PCk}),
\code{var_pct} (numeric, \% variance per PC), \code{n_genes}, \code{n_pc}.
}
\description{
Selects the top-variable features, runs \code{prcomp(t(mat), center = TRUE, scale. = FALSE)} (samples as observations), and applies a deterministic sign
convention (each PC's largest-magnitude loading made positive) so re-renders
don't mirror-flip.
}
