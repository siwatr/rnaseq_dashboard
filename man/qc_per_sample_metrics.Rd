% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/qc_helpers.R
\name{qc_per_sample_metrics}
\alias{qc_per_sample_metrics}
\title{Per-sample QC metrics}
\usage{
qc_per_sample_metrics(dds)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} with a \code{"counts"} assay.}
}
\value{
A \code{data.frame}, one row per sample (row names = \code{colnames(dds)}), with
columns \code{sample}, \code{library_size}, \code{detected}, \code{pct_mito}, \code{pct_spike}.
}
\description{
Computes, for each sample (column): library size (total counts), number of
detected features (count > 0), and the percentage of counts in mitochondrial
and in spike-in features. Uses \code{\link[scater:perCellQCMetrics]{scater::perCellQCMetrics()}} when \code{scater} is
installed; otherwise a base-R fallback returning the identical schema.
}
