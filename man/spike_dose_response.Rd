% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/ercc_helpers.R
\name{spike_dose_response}
\alias{spike_dose_response}
\title{Spike-in dose-response (per-sample titration QC)}
\usage{
spike_dose_response(
  dds,
  assay = "CPM",
  source = c("column", "mix1", "mix2"),
  conc_col = "spike_concentration",
  ref = ercc_concentrations()
)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} with spike-in features.}

\item{assay}{Observed abundance assay (linear, depth-normalized): \code{"CPM"}
(default; computed if absent), \code{"TPM"} or \code{"FPKM"} (when present).}

\item{source, conc_col, ref}{Passed to \code{\link[=resolve_spike_concentration]{resolve_spike_concentration()}}.}
}
\value{
\code{list(long, per_sample, assay)}. \code{long}: \code{sample} (factor), \code{feature},
\code{concentration}, \code{expression} (one row per spike feature x sample; zeros
kept -- drop them when plotting/fitting). \code{per_sample}: \code{sample}, \code{pct_spike},
\code{n_spike_detected}, \code{n_points}, \code{slope}, \code{r_squared}, \code{lod}.
}
\description{
For each sample, relates observed spike-in expression to the known input
concentration. Returns the long-form points (for the scatter) and a per-sample
summary (content + log-log fit). Advisory QC only.
}
