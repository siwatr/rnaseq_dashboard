% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/palette_helpers.R
\name{palette_from_json}
\alias{palette_from_json}
\title{Parse + validate a palette config from JSON}
\usage{
palette_from_json(txt)
}
\arguments{
\item{txt}{A JSON string (or file path / connection accepted by
\code{\link[jsonlite:fromJSON]{jsonlite::fromJSON()}}).}
}
\value{
A cleaned palette config list (possibly empty).
}
\description{
Inverse of \code{\link[=palette_to_json]{palette_to_json()}}. Accepts the wrapped object (\verb{\{..., palette\}})
or a bare palette object; keeps only known domains; normalizes each item's
colours to 6-digit hex (via \code{\link[=norm_color]{norm_color()}}), \code{reverse} to logical, and
\code{name}/\code{min}/\code{max} to length-1 character; drops structurally empty items.
Validation is \strong{dataset-agnostic} — kind/cardinality/conflict checks against
the live dataset are the caller's job (the module). Invalid JSON \code{stop()}s.
}
