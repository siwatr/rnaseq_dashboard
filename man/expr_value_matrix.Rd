% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr_helpers.R
\name{expr_value_matrix}
\alias{expr_value_matrix}
\title{Resolve an Expression value matrix (genes x samples) + an honest label}
\usage{
expr_value_matrix(dds, assay = "norm_logcounts")
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{assay}{A value key (\code{"vst"}, \code{"norm_logcounts"}, or a stored assay name).}
}
\value{
A list with \code{mat} (numeric genes x samples) and \code{label} (for the axis /
subtitle, reflecting any fallback).
}
\description{
Handles the two computed inputs (\code{"vst"}, \code{"norm_logcounts"}) and any stored
assay. VST is computed on the endogenous diagnostic rows (via \code{\link[=qc_vst]{qc_vst()}}) and
falls back to log-counts on any failure; \code{"norm_logcounts"} estimates
endogenous size factors when absent. Stored assays are returned as-is (the
caller's transform/pseudocount then applies on top).
}
