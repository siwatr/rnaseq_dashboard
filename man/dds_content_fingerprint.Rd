% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{dds_content_fingerprint}
\alias{dds_content_fingerprint}
\title{Content fingerprint of a dds's fit inputs}
\usage{
dds_content_fingerprint(dds)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} (or \code{NULL}).}
}
\value{
A list \code{list(rn, cn, sf)}, or \code{NULL} when \code{dds} is \code{NULL}.
}
\description{
The DESeq2 fit and the (blind) VST depend on the count matrix, the sample and
feature sets, and the size factors -- not on which \emph{derived} assays (CPM/TPM/
FPKM) happen to be present. Raw counts are immutable in this app (edits only
ever subset), so \code{rownames} + \code{colnames} fully capture "the counts changed",
and \code{sizeFactors} captures a size-factor change directly (e.g. a future
control-gene re-estimate that moves them with the counts unchanged). Keying
fit/VST staleness on this fingerprint -- rather than the coarse \code{data_version}
that \emph{any} mutation bumps -- means an assay-add preserves them while a real
structural or size-factor change invalidates them.
}
