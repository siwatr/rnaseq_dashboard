% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_helpers.R
\name{de_shrink}
\alias{de_shrink}
\title{Shrunken log2 fold-changes for a contrast (apeglm -> ashr -> normal fallback)}
\usage{
de_shrink(fit, contrast, type = c("apeglm", "ashr", "none"))
}
\arguments{
\item{fit}{A fitted \code{DESeqDataSet} (from \code{\link[=de_run]{de_run()}}).}

\item{contrast}{\code{c(variable, test, control)}.}

\item{type}{\code{"apeglm"} (default), \code{"ashr"}, or \code{"none"}.}
}
\value{
A numeric vector aligned to \code{rownames(fit)} (\code{NA} where unavailable).
}
\description{
\code{apeglm} needs a coefficient (available when \code{control} is the reference level);
\code{ashr} takes the contrast directly; \code{type="normal"} is the built-in last
resort. All optional packages are probed with \code{requireNamespace()} and every
call is wrapped so a failure degrades to \code{NA} rather than erroring.
}
