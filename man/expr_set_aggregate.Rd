% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr_helpers.R
\name{expr_set_aggregate}
\alias{expr_set_aggregate}
\title{Aggregate a gene set's expression into one value per sample}
\usage{
expr_set_aggregate(
  mat,
  ids,
  counts = NULL,
  method = c("mean", "median"),
  zscore = TRUE,
  only_expressed = TRUE,
  transform = "none",
  pseudocount = 1
)
}
\arguments{
\item{mat}{A numeric genes x samples value matrix (e.g. from
\code{\link[=expr_value_matrix]{expr_value_matrix()}}).}

\item{ids}{Authored gene-set ids (matched against \code{rownames(mat)}).}

\item{counts}{Optional raw counts matrix for the \code{only_expressed} filter;
\code{NULL} skips that filter (all present genes are used).}

\item{method}{\code{"mean"} (default) or \code{"median"} across genes, per sample.}

\item{zscore}{Per-gene z-score across samples before averaging (default TRUE).}

\item{only_expressed}{Drop genes with all-zero counts (default TRUE).}

\item{transform}{Passed to \code{\link[=expr_transform]{expr_transform()}} (\code{"none"}/\code{"log2"}/\code{"log10"}),
applied to the kept rows before z-scoring / averaging.}

\item{pseudocount}{Added before a log transform.}
}
\value{
A list: \code{values} (named per-sample numeric, or \code{NULL} when no gene
survives), \code{n_total} (authored ids), \code{n_present} (in the dataset),
\code{n_absent}, \code{n_used} (genes averaged), \code{n_nonvar} (kept genes constant
across samples -> z-score 0), \code{ids_used}, \code{method}.
}
\description{
Collapses the value-matrix rows of a gene set to a single per-sample score
(mean or median) -- the basis for the Gene Sets "Aggregate expression" plot.
Genes absent from the dataset are dropped; with \code{only_expressed = TRUE} genes
with zero counts in every sample are dropped too (needs \code{counts}). The kept
rows are transformed (\code{transform}/\code{pseudocount}), optionally per-gene z-scored
across samples (the default, so highly-expressed genes do not dominate the
average), then averaged down to one vector. All the accounting is returned so
the caller can caption "\if{html}{\out{<n>}} of \if{html}{\out{<N>}} genes" and warn about z-scored constant
genes.
}
