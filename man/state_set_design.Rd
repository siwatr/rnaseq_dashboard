% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/state.R
\name{state_set_design}
\alias{state_set_design}
\title{Set the model design (and optional reference-level relevels) -- design-scoped}
\usage{
state_set_design(state, design, relevel = NULL, action = list())
}
\arguments{
\item{state}{App-state object.}

\item{design}{A one-sided model formula (e.g. \code{~ condition + batch}).}

\item{relevel}{Optional named list \code{col = ref} of reference levels to set
(via \code{\link[=de_relevel]{de_relevel()}}) before applying the design.}

\item{action}{Extra named fields to merge into the history entry.}
}
\value{
\code{state}, invisibly.
}
\description{
Updates \code{design(working)} and any factor reference levels \strong{without} bumping
\code{data_version}, so design-independent caches (PCA/VST/QC) survive; bumps a
separate \code{design_version} that the DE fit keys on. Logs to history. Not pushed
onto the undo stack (the design is directly re-editable). The design formula
does not change sample/feature values, and a relevel only reorders factor
levels, so nothing downstream except DE actually depends on it.
}
