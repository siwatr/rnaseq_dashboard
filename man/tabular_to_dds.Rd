% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/load_helpers.R
\name{tabular_to_dds}
\alias{tabular_to_dds}
\title{Build a DESeqDataSet from a counts table and a sample sheet}
\usage{
tabular_to_dds(
  counts_df,
  sample_df,
  id_col = NULL,
  sample_id_col = NULL,
  design = ~1
)
}
\arguments{
\item{counts_df}{Feature-by-sample counts (data.frame/matrix). Feature ids are
taken from \code{id_col}, else the first non-numeric column, else rownames.}

\item{sample_df}{Sample sheet (one row per sample).}

\item{id_col}{Optional feature-id column name in \code{counts_df}.}

\item{sample_id_col}{Optional sample-id column name in \code{sample_df}.}

\item{design}{Model formula (default \code{~ 1}).}
}
\value{
A \code{DESeqDataSet}. Errors (listing mismatches) if sample ids and counts
columns don't match.
}
\description{
Build a DESeqDataSet from a counts table and a sample sheet
}
