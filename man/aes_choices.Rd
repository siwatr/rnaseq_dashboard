% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/aes_helpers.R
\name{aes_choices}
\alias{aes_choices}
\title{Grouped selectInput choices from a catalog}
\usage{
aes_choices(
  catalog,
  kinds = c("discrete", "continuous"),
  none = FALSE,
  max_levels = NULL,
  state = NULL
)
}
\arguments{
\item{catalog}{Output of \code{\link[=aes_catalog]{aes_catalog()}}.}

\item{kinds}{Keep attributes whose \code{kind} is in this set (default both).}

\item{none}{Prepend a "General" group with a single \code{(none)} entry.}

\item{max_levels}{For discrete attributes, drop those with more than this many
levels (uses the live data; \code{NULL} = no cap). Used for the shape selector.}

\item{state}{Needed only when \code{max_levels} is set (to read level counts).}
}
\value{
A named list of \verb{<optgroup>}s for \code{selectInput(choices = )}, ordered
General -> This session -> Data metadata (see \code{\link[=group_field_choices]{group_field_choices()}}).
}
\description{
Grouped selectInput choices from a catalog
}
