% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/filter_helpers.R
\name{flag_features}
\alias{flag_features}
\title{Flag low-quality features for removal}
\usage{
flag_features(
  dds,
  use_filter_by_expr = TRUE,
  group = NULL,
  min_count = 10,
  min_samples = NULL
)
}
\arguments{
\item{dds}{A \code{DESeqDataSet} with a \code{"counts"} assay.}

\item{use_filter_by_expr}{Use \code{edgeR::filterByExpr()} when available
(default \code{TRUE}); falls back to the manual rule otherwise.}

\item{group}{Optional \code{colData} column giving the grouping for
\code{filterByExpr}; defaults to the design / first discrete column.}

\item{min_count}{Manual rule only: minimum total count across samples.
(Not forwarded to \code{filterByExpr}, whose \code{min.count} is per-sample and would
mean something different; \code{filterByExpr} uses its own design-aware default.)}

\item{min_samples}{Manual rule only: minimum number of samples with count > 0
(default \code{NULL} = not applied).}
}
\value{
A \code{data.frame}, one row per feature, with \code{feature_id},
\code{feature_class}, \code{total_count}, \code{n_detected}, \code{mean_logcounts},
\code{drop_all_zero}, \code{fails_min_count}, \code{fails_min_samples},
\code{fails_filterByExpr}, \code{suggested_drop}, and \code{reason}.
}
\description{
Computes, per feature, expression-based drop suggestions. Always flags
all-zero endogenous features; then either \code{\link[edgeR:filterByExpr]{edgeR::filterByExpr()}} (the
design-aware default, when \code{edgeR} is installed) or a manual
total-count / detected-samples rule. Spike-in and exogenous features are
\strong{exempt} (\code{suggested_drop = FALSE} always) - they are flagged on the
Feature tab elsewhere but never dropped by expression filtering.
}
