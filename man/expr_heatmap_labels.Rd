% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr_helpers.R
\name{expr_heatmap_labels}
\alias{expr_heatmap_labels}
\title{Display labels for heatmap rows or columns (duplicate- and NA-safe)}
\usage{
expr_heatmap_labels(
  keys,
  source = "__id__",
  meta = NULL,
  meta_keys = rownames(meta)
)
}
\arguments{
\item{keys}{The matrix dimnames to label (gene ids for rows, sample ids for
columns).}

\item{source}{\code{"__id__"} to use \code{keys} verbatim, or a \code{meta} column name.}

\item{meta}{The \code{rowData}/\code{colData} data frame (or \code{NULL}).}

\item{meta_keys}{Row keys of \code{meta} aligned to the dds (default
\code{rownames(meta)}); \code{keys} are matched against these.}
}
\value{
A character vector the same length/order as \code{keys}.
}
\description{
Builds the vector of \emph{display} labels for a set of matrix dimnames without
touching the matrix's actual \code{dimnames} -- the matrix keeps the unique dds ids
as its row/column names (the stable key for subsetting, \code{anno_mark} indexing,
and clustering), while ComplexHeatmap's \verb{row_labels=}/\verb{column_labels=} receive
this vector. Because it is display-only, duplicate labels (many gene ids share
one \code{gene_name}) are fine. When \code{source} is a metadata column, a row whose value
is \code{NA}/empty falls back to its id, so every element still gets a label.
}
