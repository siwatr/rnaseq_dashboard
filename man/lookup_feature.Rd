% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_lookup.R
\name{lookup_feature}
\alias{lookup_feature}
\title{Resolve queries to feature IDs by name}
\usage{
lookup_feature(
  query,
  row_data,
  ids = rownames(row_data),
  feature_type = NULL,
  column = NULL,
  case_insensitive = FALSE
)
}
\arguments{
\item{query}{Character vector of names (or IDs) to look up.}

\item{row_data}{A data.frame-like of feature metadata (one row per feature).}

\item{ids}{Character vector of feature IDs, one per row of \code{row_data}
(default \code{rownames(row_data)}).}

\item{feature_type}{Feature type (e.g. "gene"); the name column searched is
\code{paste0(feature_type, "_name")} unless \code{column} is given.}

\item{column}{Explicit name column to search; overrides \code{feature_type}.}

\item{case_insensitive}{Match case-insensitively (default \code{FALSE}).}
}
\value{
Character vector of matched feature IDs (NA where unmatched), same
length as \code{query}.
}
\description{
Resolve queries to feature IDs by name
}
