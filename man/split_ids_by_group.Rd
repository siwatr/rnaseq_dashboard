% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_set_helpers.R
\name{split_ids_by_group}
\alias{split_ids_by_group}
\title{Split feature ids into groups by annotation column(s)}
\usage{
split_ids_by_group(df, id_col, anno_cols = character(0), sep = ".")
}
\arguments{
\item{df}{A data.frame carrying the id column and the annotation column(s).
Subset it to the rows you want \emph{before} calling.}

\item{id_col}{Name of the column holding feature ids.}

\item{anno_cols}{Character vector of column names whose combined values key
the groups. Empty (the default) yields a single group named \code{"All"}.}

\item{sep}{Separator joining multiple annotation values (default \code{"."}).}
}
\value{
A named list of unique id vectors, one per group. \code{NA} / blank ids are
dropped, as are groups left with no ids; \code{NA} annotation values key as
\code{"NA"}. Returns an empty list when nothing survives.
}
\description{
Powers the Gene Sets table import: an imported table (e.g. a DESeq2 result
sheet from another analysis) is sliced into \strong{one gene set per annotation
group} -- filter out \code{no_change}, split by direction, get \code{up} / \code{down} sets.
The group key is the annotation columns' values joined by \code{sep}.
}
