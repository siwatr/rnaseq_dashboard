% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_set_helpers.R
\name{gene_set_overlap_list}
\alias{gene_set_overlap_list}
\title{Named id-lists for a set-overlap comparison (Euler / Venn / UpSet)}
\usage{
gene_set_overlap_list(sets, feature_ids = NULL, within = FALSE)
}
\arguments{
\item{sets}{A named list of gene-set records (or bare id vectors).}

\item{feature_ids}{Current dataset feature ids (\code{rownames(dds)}).}

\item{within}{When TRUE, one "present" row per set (present ids only); else a
present + absent breakdown of the full authored membership (stacked bar).}
}
\value{
A named list of unique id vectors, one per set (a set that
contributes nothing is kept as \code{character(0)} so callers can report it).
}
\description{
Named id-lists for a set-overlap comparison (Euler / Venn / UpSet)
}
