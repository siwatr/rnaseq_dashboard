% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/de_helpers.R
\name{de_contrast_validity}
\alias{de_contrast_validity}
\title{Validity of a stored contrast against the current dds}
\usage{
de_contrast_validity(dds, spec)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{spec}{A contrast spec \code{list(var, test, control, ...)}.}
}
\value{
One of \code{"extractable"}, \code{"not_in_design"}, \code{"invalid"}.
}
\description{
Distinguishes three states so the UI can label + decide extraction:
\code{"extractable"} (factor is in the design and both levels exist -> can produce
results now), \code{"not_in_design"} (factor + levels exist but the factor isn't a
current design term -> kept; add it to the design to extract), and \code{"invalid"}
(the factor column or a level no longer exists in the data -> kept unless
removed; recoverable if restored).
}
