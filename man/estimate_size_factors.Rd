% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/assay_helpers.R
\name{estimate_size_factors}
\alias{estimate_size_factors}
\alias{estimate_size_factors_endogenous}
\title{Estimate size factors under a control-gene / type config}
\usage{
estimate_size_factors(dds, config = default_sizefactor_config())

estimate_size_factors_endogenous(dds)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{config}{A config list (default \code{\link[=default_sizefactor_config]{default_sizefactor_config()}}).}
}
\value{
\code{dds} with \code{sizeFactors} set and the config recorded.

\code{estimate_size_factors_endogenous()}: \code{dds} with endogenous-control
size factors (a thin wrapper kept for the many internal callers).
}
\description{
Generalizes \code{\link[=estimate_size_factors_endogenous]{estimate_size_factors_endogenous()}}: the control-gene set is
chosen by \code{config$control} (\code{"endogenous"}/\code{"spike_in"}/\code{"custom"}) and the
estimator by \code{config$type}. Keeps the poscounts fallback -- a \code{type = "ratio"}
run that \strong{fails when every control gene has at least one zero} (all geometric
means zero, common on sparse data) retries with \code{type = "poscounts"}. Records
the config on the returned dds so the choice travels with the object.
}
