% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_set_helpers.R
\name{gene_set_commit}
\alias{gene_set_commit}
\title{Commit ids into a named gene set (New or Append)}
\usage{
gene_set_commit(sets, name, ids, mode = c("new", "append"), source = "manual")
}
\arguments{
\item{sets}{The current named list of gene-set records (\code{state$gene_sets}).}

\item{name}{Target set name (trimmed; must be non-empty).}

\item{ids}{Feature ids to add.}

\item{mode}{\code{"new"} or \code{"append"}.}

\item{source}{Provenance label for a newly created set.}
}
\value{
A list \code{list(sets, name)} — the updated store and the resolved name
(which may differ from \code{name} when a New collision was auto-suffixed).
}
\description{
\code{mode = "new"} creates a set under \code{name}, auto-suffixing (\code{name_2}, \code{name_3},
...) if the name is taken so an existing set is never silently overwritten.
\code{mode = "append"} unions the ids into the existing \code{name} (deduplicated), or
creates it when absent.
}
