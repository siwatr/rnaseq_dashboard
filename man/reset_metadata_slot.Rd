% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/metadata_helpers.R
\name{reset_metadata_slot}
\alias{reset_metadata_slot}
\title{Reset one metadata slot to the originally loaded values}
\usage{
reset_metadata_slot(working, original, slot = c("colData", "rowData"))
}
\arguments{
\item{working}{The current \code{DESeqDataSet}.}

\item{original}{The originally loaded \code{DESeqDataSet} (the reset target).}

\item{slot}{\code{"colData"} (samples) or \code{"rowData"} (features).}
}
\value{
\code{working} with that slot reverted to \code{original}.
}
\description{
Slot-scoped "reset to original": restores \code{colData} or \code{rowData} of \code{working}
to the values in \code{original}, \strong{by id, for the rows currently present} in
\code{working} — so it does not undo sample/feature \emph{filtering}. The original
column schema wins (user-added columns are dropped). For \code{colData} the design
formula is restored from \code{original} (so it cannot reference a column that no
longer exists); for \code{rowData}, normalized assays are refreshed and size
factors re-estimated when set (feature_length / feature_class feed TPM/FPKM
and \code{controlGenes}). Rows whose id is absent from \code{original} (e.g. a renamed
sample) keep their current values for the original-schema columns.
}
