% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/expr_helpers.R
\name{expr_long_frame}
\alias{expr_long_frame}
\title{Assemble the single-gene long data frame}
\usage{
expr_long_frame(values, groups, samples = names(values), colour = NULL)
}
\arguments{
\item{values}{Named (or same-order) numeric vector of the gene's values.}

\item{groups}{Per-sample grouping (character/factor), same length/order as
\code{values}.}

\item{samples}{Sample ids (same length/order); defaults to \code{names(values)}.}

\item{colour}{Optional per-sample colour attribute (same length/order).}
}
\value{
A data.frame with \code{sample}, \code{group} (factor), \code{value}, and (if given)
\code{colour}.
}
\description{
Joins one gene's per-sample values to a grouping vector (and optional colour
vector), dropping samples with a missing group. The grouping keeps its factor
level order when supplied as a factor.
}
