% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gene_set_helpers.R
\name{combine_gene_set_annotation}
\alias{combine_gene_set_annotation}
\title{Combine several gene sets into one id -> label annotation}
\usage{
combine_gene_set_annotation(
  sets,
  shared = c("concat", "label", "first"),
  sep = ";",
  label = "multiple"
)
}
\arguments{
\item{sets}{A \strong{named} list of gene-set records (or bare id vectors); each
name is the label for genes in that set. \code{NULL} / empty yields empty output.}

\item{shared}{How to label a gene in >1 set (see above).}

\item{sep}{Separator for \code{shared = "concat"} (default \code{";"}).}

\item{label}{Level name for \code{shared = "label"} (default \code{"multiple"}).}
}
\value{
A list \code{list(annotation, levels, shared_ids, note)} -- \code{annotation} a
named character vector (id -> label), \code{levels} the labels in display order
(member-set names in input order, then any overlap labels in first-appearance
order), \code{shared_ids} the genes in >1 set, \code{note} a human summary string.
}
\description{
The P7 "annotated" layer: a second-level object grouping several member gene
sets, where each gene's label = the member set it belongs to. Non-destructive
-- it reads each set's authored ids and produces one label per gene over the
union. Feeds \verb{new_gene_set(kind = "annotated", annotation = <result$annotation>)}.
}
\details{
A gene in more than one member set is resolved by \code{shared}:
\itemize{
\item \code{"concat"} (default): the member-set names joined by \code{sep} (in input order),
e.g. \code{"up;hypoxia"} -- stays one label per gene, so \code{row_split} / saving keep
working, and overlaps read as their own level.
\item \code{"label"}: a single distinct level named \code{label} (default \code{"multiple"}).
\strong{Rejected when \code{label} collides with a member-set name} (it would silently
merge a real set into the overlap bucket).
\item \code{"first"}: the first member set (in input order) the gene appears in.
}
}
