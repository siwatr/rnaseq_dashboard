% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/dimreduc_helpers.R
\name{pca_input}
\alias{pca_input}
\title{Build the PCA input matrix for an assay (endogenous-only) + an honest label}
\usage{
pca_input(dds, assay = "vst", log_transform = FALSE)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{assay}{One of \code{"vst"} (default), \code{"norm_logcounts"}, or a stored assay
name (e.g. \code{"logcounts"}, \code{"CPM"}).}

\item{log_transform}{Apply \code{log2(x + 1)} to a \emph{stored} assay (ignored for the
computed \code{"vst"} / \code{"norm_logcounts"} inputs, which are already on a log/
stabilized scale).}
}
\value{
A list with \code{mat} (genesxsamples numeric, endogenous rows only) and
\code{label} (for the plot subtitle, reflecting any fallback/transform).
}
\description{
Build the PCA input matrix for an assay (endogenous-only) + an honest label
}
