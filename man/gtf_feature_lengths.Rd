% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtf_helpers.R
\name{gtf_feature_lengths}
\alias{gtf_feature_lengths}
\title{Union feature length over a chosen GTF \code{type}}
\usage{
gtf_feature_lengths(gtf, type = "exon", group_col = "gene_id")
}
\arguments{
\item{gtf}{A \code{GRanges} from \code{\link[=import_gtf]{import_gtf()}}.}

\item{type}{Feature type to measure (default \code{"exon"}).}

\item{group_col}{Attribute to group by (default \code{"gene_id"}).}
}
\value{
Named numeric vector of lengths (names = \code{group_col} values).
}
\description{
Keeps rows of the given \code{type}, groups by \code{group_col}, reduces overlapping /
adjacent ranges, and sums their widths -- the effective length for TPM/FPKM.
Pick \code{type} to match how reads were quantified: \code{exon} for mature mRNA, or
\code{gene}/\code{transcript} when reads fall outside exons (e.g. nascent transcripts).
}
