% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_lookup.R
\name{suggest_features}
\alias{suggest_features}
\title{Suggest feature names for a near-miss query ("Did you mean ...?")}
\usage{
suggest_features(query, values, n = 5L, cap = 100L, min_chars = 2L)
}
\arguments{
\item{query}{A single search string.}

\item{values}{Character vector of searchable values (e.g. feature names).}

\item{n}{Maximum number of suggestions to return (default 5).}

\item{cap}{Raw-match ceiling above which the result is "too broad" (default 100).}

\item{min_chars}{Minimum query length to search (default 2).}
}
\value{
A list: \code{suggestions} (ranked, de-duplicated, up to \code{n}), \code{n_match}
(distinct matches), and \code{over_cap} (logical, raw matches exceeded \code{cap}).
}
\description{
On an exact-match miss, offers ranked partial matches. Matching is \strong{always
case-insensitive} (independent of any case-sensitive lookup toggle), so a
case-sensitive search for \code{"duxf3"} still suggests \code{"Duxf3"}. Matches are
ranked by position -- prefix (\verb{^q}) first, then word-start (\code{q} after a
non-alphanumeric), then any substring -- with ties broken by shorter length
then alphabetically. Guards keep it cheap: a query under \code{min_chars}
characters yields nothing, and more than \code{cap} raw matches returns
\code{over_cap = TRUE} (too broad to list).
}
