% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils_table.R
\name{dt_table}
\alias{dt_table}
\title{Standard read-only table for the dashboard}
\usage{
dt_table(
  df,
  page_length = 10L,
  length_menu = c(10, 25, 50, 100),
  filter = "top",
  scrollX = TRUE,
  selection = "none",
  options = list(),
  ...
)
}
\arguments{
\item{df}{A data frame to display.}

\item{page_length}{Initial number of rows per page.}

\item{length_menu}{Row-count choices offered in the rows-per-page selector.}

\item{filter}{DT column-filter placement; \code{"top"} (default) or \code{"none"}.}

\item{scrollX}{Enable horizontal scrolling for wide tables.}

\item{selection}{Row-selection mode passed to \code{\link[DT:datatable]{DT::datatable()}}. Defaults to
\code{"none"} so display tables are read-only (DT's own default is \code{"multiple"},
which makes rows selectable); pass e.g. \code{list(mode = "multiple")} for the
actionable filtering tables.}

\item{options}{Extra DataTables options, merged over (and overriding) the
defaults (\code{dom}, \code{pageLength}, \code{lengthMenu}, \code{scrollX}).}

\item{...}{Further arguments passed to \code{\link[DT:datatable]{DT::datatable()}}.}
}
\value{
A \code{datatables} htmlwidget.
}
\description{
A thin wrapper over \code{\link[DT:datatable]{DT::datatable()}} applying the repo conventions for
read-only display tables: per-column filters (\code{filter = "top"}), a
rows-per-page selector (\code{lengthMenu}), a search box, horizontal scrolling,
and hidden row names.
}
