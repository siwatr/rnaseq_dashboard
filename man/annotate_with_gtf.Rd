% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/gtf_helpers.R
\name{annotate_with_gtf}
\alias{annotate_with_gtf}
\title{Annotate features from a GTF (authoritative over OrgDb)}
\usage{
annotate_with_gtf(
  dds,
  gtf,
  match_col = "auto",
  import_cols = NULL,
  compute_length = FALSE,
  length_type = "exon",
  feature_type = "gene"
)
}
\arguments{
\item{dds}{A \code{DESeqDataSet}.}

\item{gtf}{A \code{GRanges} from \code{\link[=import_gtf]{import_gtf()}}.}

\item{match_col}{GTF column matched to rownames; \code{"auto"} (id then name).}

\item{import_cols}{Attribute columns to copy into \code{rowData} (character).}

\item{compute_length}{Whether to compute and store \code{feature_length}.}

\item{length_type}{Feature \code{type} used for length (default \code{"exon"}).}

\item{feature_type}{Feature unit; sets the name column \verb{<feature_type>_name}.}
}
\value{
list(\code{dds}, \code{report} = list(\code{matched}, \code{total}, \code{length_set}, \code{length_complete})).
}
\description{
Matches dds rownames to a GTF column, overlays the selected attribute columns
onto \code{rowData} (filling where the GTF has a value, keeping existing values
elsewhere), and -- when \code{compute_length} -- writes union-\code{length_type}
lengths to \code{rowData$feature_length}. \code{gene_name} is stored as
\verb{<feature_type>_name} and \code{seqnames} as \code{chromosome}; other columns keep their
names. \code{feature_class} and counts are untouched.
}
